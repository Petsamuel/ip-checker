import Header from "../components/Header";

interface Props {
  text: string;
}

function Home({ text }: Props) {
  return (
    <section className="">
      <Header title="IP Address Tracker" />
      section 2
    </section>
  );
}

export default Home;
