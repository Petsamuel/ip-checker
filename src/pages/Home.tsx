import Header from "../components/Header";

interface Props {
  text: string;
}

function Home({ text }: Props) {
  return (
    <section>
      <Header title="IP Address Tracker" />
          section 2
    </section>
  );

}

export default Home;
